### Шаг 1. Собери «пельменный набор» (подготовка)

Тебе понадобятся:  
- **Компьютер** (пусть даже старый, как легенда о Шлёпе).  
- **Node.js** (скачай с [nodejs.org](https://nodejs.org) — это вроде теста на прочность для твоего ПК).  
- **Текстовый редактор** (VS Code, Notepad++ — что угодно, лишь бы не на салфетке писать).  

**Проверь, что Node.js установлен:**  
Открой терминал (командную строку) и введи:  
```bash
node -v
npm -v
```  
Если видишь цифры (например, `v18.17.0`) — ты молодец! Если ошибка — иди качай Node.js.  

---

### Шаг 2. Создай «пельменную фабрику» (структура проекта)

1. Сделай папку с названием, например, `shlepa-pelmeni`.  
2. Внутри создай такие папки и файлы:  
   ```
   shlepa-pelmeni/
   ├── public/              # Здесь будет «витрина» с пельменями (фронтенд)
   │   └── index.html     # HTML‑код (как рецепт пельменей)
   ├── uploads/             # Склад для «пельменей» (загруженных файлов)
   └── server.js           # «Повар» (сервер на Node.js)
   ```

**Важно!** Папка `uploads/` должна быть пустой — туда будут «падать» пельмени (файлы).

---

### Шаг 3. Установи «пельменные специи» (зависимости)

1. Открой терминал в папке `shlepa-pelmeni`.  
2. Введи команду:  
   ```bash
   npm install express multer
   ```  
   Это как добавить в пельмени:  
   - `express` — соус (веб‑сервер);  
   - `multer` — приправа для загрузки файлов.  

---

### Шаг 4. Настрой «рецепт пельменей» (фронтенд)

1. Открой файл `public/index.html` в редакторе.  
2. Найди строку:  
   ```javascript
   const BASE_URL = 'http://ваш-сервер:3000';
   ```  
3. Замени её на:  
   - Если запускаешь на своём ПК:  
     ```javascript
     const BASE_URL = 'http://localhost:3000';
     ```  
   - Если хочешь, чтобы другие видели твои «пельмени» в сети:  
     ```javascript
     const BASE_URL = 'http://<твой-IP>:3000';  // например, 'http://192.168.1.100:3000'
     ```  

**Как узнать свой IP?**  
- Windows: открой командную строку и введи `ipconfig`.  
- macOS/Linux: в терминале напиши `ifconfig` или `ip addr`.  

---

### Шаг 5. «Сварить пельмени» (запуск сервера)

1. В терминале (в папке `shlepa-pelmeni`) введи:  
   ```bash
   node server.js
   ```  
2. Если всё ок, увидишь:  
   ```
   Сервер запущен на http://localhost:3000
   ```  
   Это значит, что «пельмени» уже томятся на плите!  

---

### Шаг 6. «Подать пельмени к столу» (открытие в браузере)

1. **На своём ПК:**  
   - Открой браузер и перейди по ссылке:  
     `http://localhost:3000`  

2. **С другого устройства в сети:**  
   - Введи в браузере:  
     `http://<твой-IP>:3000` (например, `http://192.168.1.100:3000`).  

Ты увидишь:  
- Пустую «тарелку» (папку `uploads/`).  
- Форму для загрузки «пельменей» (файлов).  

---

### Шаг 7. «Попробовать пельмени» (проверка работы)

1. Нажми кнопку **«Загрузить файл»** и выбери любой файл (например, фото Шлёпы).  
2. Посмотри, как он «упал» в папку `uploads/`.  
3. Попробуй кликнуть на папку — если создашь подпапку, она тоже отобразится.  

**Поздравляем!** Ты запустил свой сетевой «пельменный проводник»! 🍴

---

### Шаг 8. «Дополнительные приправы» (по желанию)

- **Хочешь другой порт?**  
  В `server.js` поменяй:  
  ```javascript
  const PORT = 3000;  // на, например, 8080
  ```  
  Потом открывай `http://localhost:8080`.  

- **Чтобы сервер работал вечно:**  
  Установи `pm2`:  
  ```bash
  npm install -g pm2
  pm2 start server.js
  ```  

- **Безопасность (чтобы чужие не съели твои пельмени):**  
  Добавь пароль (например, через `express-basic-auth`).  

---

### Если что‑то пошло не так («пельмени развалились»)

1. **«Cannot get /»**  
   - Проверь, что `index.html` лежит в `public/`.  
   - Перезапусти сервер: `node server.js`.  

2. **«Connection refused»**  
   - Убедиться, что порт `3000` свободен.  
   - Проверить IP‑адрес.  

3. **Файлы не загружаются**  
   - Папка `uploads/` существует?  
   - У Node.js есть права на запись?  

4. **Другие устройства не видят «пельмени»**  
   - Все в одной сети (роутер один)?  
   - Брандмауэр не блокирует порт `3000`?  

---

### Итог

Ты:  
- Запустил веб‑проводник с «пельменной» тематикой.  
- Научился хранить файлы в локальной сети.  
- Получил право гордо называть себя **«Мастер Пельменей»**.  
